export interface Dropdown {
    items: any[];  // List of items to display
    valueField: string;  // Key to use as the value
    labelField: string;  // Key to display in the dropdown
    placeholder?: string;  // Placeholder text
    heading?: string;  // Optional heading for the dropdown
  }
  